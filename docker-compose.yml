services:
  db:
    image: postgres:16
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-rankify}:${POSTGRES_PASSWORD:-rankify}@db:5432/${POSTGRES_DB:-rankify}
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      - db
    volumes:
      - ./backend:/app
      - backend-venv:/app/.venv

  frontend:
    build:
      context: ./frontend
      target: dev
    env_file:
      - ./frontend/.env
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    ports:
      - "${FRONTEND_PORT:-5173}:5173"

volumes:
  db-data:
  backend-venv:
  frontend-node-modules:
